@rendermode @(InteractiveServer.DisablePreRender())

<div class="navmenu">
    <input type="checkbox" id="navmenu-toggle" />
    <nav class="sitenav" aria-labelledby="main-menu" onclick="document.getElementById('navmenu-toggle').click();">
        <FluentNavMenu Id="main-menu"
            Collapsible="true" Width="200"
            Title="Navigation menu"
            @bind-Expanded="IsExpanded">

            <FluentNavLink Href="@PageRoute.Home"
                Match="NavLinkMatch.All"
                Icon="@(new Icons.Regular.Size20.Home())"
                IconColor="Color.Accent">
                Home</FluentNavLink>

            <AuthorizeView Policy="@Policies.RolePlanningAssetCoverage">
                <FluentNavLink Href="@PageRoute.CrmImport"
                    Icon="@(new Icons.Regular.Size20.DocumentAdd())"
                    IconColor="Color.Accent">
                    Import CRM</FluentNavLink>

                <FluentNavLink Href="@PageRoute.CrmImportVerification"
                    Icon="@(new Icons.Regular.Size20.DocumentCheckmark())"
                    IconColor="Color.Accent">
                    Verifikasi CRM</FluentNavLink>
            </AuthorizeView>

            <FluentNavLink Href="@PageRoute.WorkloadShared"
                Icon="@(new Icons.Regular.Size20.DocumentTable())"
                IconColor="Color.Accent">
                Workload</FluentNavLink>

            <AuthorizeView Policy="@Policies.RoleHelpdesk">
                <FluentNavLink Href="@PageRoute.HelpdeskStaging"
                    Icon="@(new Icons.Regular.Size20.PersonDesktop())"
                    IconColor="Color.Accent">
                    Helpdesk Staging</FluentNavLink>

                <FluentNavLink Href="@PageRoute.Helpdesk"
                    Icon="@(new Icons.Regular.Size20.CheckboxPerson())"
                    IconColor="Color.Accent">
                    Helpdesk</FluentNavLink>
            </AuthorizeView>

            <AuthorizeView Policy="@Policies.RolePlanningAssetCoverage">
                <FluentNavLink Href="@PageRoute.ApprovalStaging"
                    Icon="@(new Icons.Regular.Size20.DocumentAdd())"
                    IconColor="Color.Accent">
                    Approval Staging</FluentNavLink>

                <FluentNavLink Href="@PageRoute.Approval"
                    Icon="@(new Icons.Regular.Size20.DocumentCheckmark())"
                    IconColor="Color.Accent">
                    Approval</FluentNavLink>
            </AuthorizeView>

            <AuthorizeView Policy="@Policies.AdministratorPrivilege">
                <FluentNavLink Href="@PageRoute.Settings" Icon="@(new Icons.Regular.Size20.Settings())" IconColor="Color.Accent">Settings</FluentNavLink>
            </AuthorizeView>

        </FluentNavMenu>
    </nav>
</div>
