@if (Models is not null)
{
    <div class="@_wrapperCss" style="width: 100%;">
        <table class="aging-tabulation" style="width: 100%;">
            <thead>
                <tr>
                    <th class="tabulation-header" style="">
                        PAC Username
                    </th>
                    <th class="tabulation-header" style="min-width: 100px;">
                        AVG
                    </th>
                    <th class="tabulation-header" style="min-width: 100px;">
                        MIN
                    </th>
                    <th class="tabulation-header" style="min-width: 100px;">
                        MAX
                    </th>
                    <th class="tabulation-header" style="">
                        Verifikasi Total
                    </th>
                    <th class="tabulation-header" style="">
                        Total Reject
                    </th>
                    <th class="tabulation-header" style="">
                        Total Verified
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var model in Models)
                {
                    <tr>
                        <td class="tabulation-header-row">@model.Username</td>
                        <td class="tabulation-content">@model.GetDisplayAverageAging()</td>
                        <td class="tabulation-content">@model.GetDisplayMinAging()</td>
                        <td class="tabulation-content">@model.GetDisplayMaxAging()</td>
                        <td class="tabulation-content">@model.GetDisplayVerificationTotal()</td>
                        <td class="tabulation-content">@model.GetDisplayTotalReject()</td>
                        <td class="tabulation-content">@model.GetDisplayTotalVerified()</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}

@code
{
    private string _wrapperCss => IsPageView
        ? "tabulation-wrapper-page-view"
        : "tabulation-wrapper";
}