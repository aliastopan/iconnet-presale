@using ApexCharts
@using IConnet.Presale.WebApp.Models.Presales.Reports

@if (Models.Count != 0)
{
    <ApexChart TItem="RootCauseReportModel"
        Options="Options">

        <ApexPointSeries TItem="RootCauseReportModel"
            Items="Models"
            Name=""
            SeriesType="SeriesType.Pie"
            XValue="@(e => e.RootCause.TruncateWithEllipsis(24))"
            YAggregate="@(e => e.Sum(x => x.GrandTotal))"
            ShowDataLabels />

    </ApexChart>
}
